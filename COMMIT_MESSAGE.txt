feat: Implement undo/redo UI and keyboard shortcuts (Step 3 complete)

Add comprehensive undo/redo functionality with buttons, keyboard shortcuts,
and visual feedback to complete Step 3 of Milestone 2.

### Features Added:
- Undo/redo buttons in toolbar with proper disabled states
- Keyboard shortcuts: Ctrl+Z (undo), Ctrl+Y/Ctrl+Shift+Z (redo)
- Dynamic tooltips showing next command to undo/redo
- Status bar integration with action feedback messages
- Cross-platform key support (Ctrl on Windows/Linux, Cmd on Mac)

### UI Enhancements:
- Added toolbar separator for visual grouping
- Disabled button styling with reduced opacity
- Event-driven button state updates
- Graceful handling of missing DOM elements

### Technical Implementation:
- Extended CommandHistory with helper methods (canUndo, canRedo, getUndoStack, getRedoStack)
- Integrated keyboard shortcuts with existing initKeyboardShortcuts()
- Added updateUndoRedoButtons() function for state management
- Status messages for execute/undo/redo operations

### Files Modified:
- index.html: Added undo/redo buttons to toolbar
- styles/main.css: Added disabled states and toolbar separator
- src/app.js: Added keyboard shortcuts and button handlers
- src/commands/CommandHistory.js: Added UI helper methods
- docs/milestone2.md: Updated progress tracking

### Testing:
- Added comprehensive test suite (20 tests)
- Covers button states, keyboard shortcuts, status messages
- Tests edge cases and command merging scenarios
- All 599 tests passing (579 existing + 20 new)

### Success Criteria Met:
✅ Undo/redo buttons work correctly
✅ Keyboard shortcuts functional (Ctrl+Z, Ctrl+Y)
✅ Visual feedback appropriate
✅ Status messages helpful
✅ 20 comprehensive UI tests

Ready for Step 4: Grid Resize Infrastructure
