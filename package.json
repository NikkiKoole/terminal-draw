{
  "name": "terminal-draw",
  "version": "0.1.0",
  "description": "A web-based ASCII drawing tool for terminal art",
  "type": "module",
  "scripts": {
    "dev": "bun --bun vite",
    "build": "bun --bun vite build",
    "preview": "bun --bun vite preview",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:run": "bun test",
    "test:summary": "bun test 2>&1 | tail -3",
    "build:check": "bun run build > /dev/null 2>&1 && echo '✅ Build successful' || echo '❌ Build failed'",
    "lint": "bun scripts/lint-check.js",
    "lint:fix": "echo '🔧 Auto-fix not implemented yet'",
    "lint:check": "bun scripts/lint-check.js",
    "check:all": "bun run test:summary && bun run build:check && bun run lint:check",
    "show:structure": "bash scripts/show-structure.sh"
  },
  "devDependencies": {
    "jsdom": "^27.4.0",
    "vite": "^5.0.0"
  },
  "keywords": [
    "ascii",
    "terminal",
    "drawing",
    "art",
    "editor"
  ],
  "author": "",
  "license": "MIT"
}
