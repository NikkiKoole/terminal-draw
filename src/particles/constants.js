/**
 * Particle system constants
 */

// Default density glyphs: light to dense (░▒▓█)
export const DEFAULT_DENSITY_GLYPHS = ["\u2591", "\u2592", "\u2593", "\u2588"];

// Default cycle duration in ms
export const DEFAULT_CYCLE_DURATION = 500;

// Despawn margin (cells beyond scene bounds before particle is removed)
export const DESPAWN_MARGIN = 2;
